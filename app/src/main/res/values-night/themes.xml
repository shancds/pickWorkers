<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PickWorkers" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/cardText_color</item>
        <item name="colorPrimaryDark">@color/card_color</item>
        <item name="colorAccent">@color/card_color</item>
        <!-- Customize your theme here. -->
    </style>
</resources>